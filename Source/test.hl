import basics.hl

var a
var b
var length
var swap

set dir length int 10
set dir a int 0
set dir b int 1

unt dir length
add dir a dir b dir swap
cpy dir swap dir a
add dir a dir b dir swap
cpy dir swap dir b
inc dir length int -1
end_unt

out dir a int
